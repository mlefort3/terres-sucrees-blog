---
export interface Props {
	date: Date
}

const { date } = Astro.props
---

<time class='text-sm text-opacity-60' datetime={date.toISOString()}>
	{
		date.toLocaleDateString('fr-fr', {
			year: 'numeric',
			month: 'long',
			day: 'numeric'
		})
	}
</time>
